// Sample ini (initialization) file. This shows the format that
// the file uses. See the documentation for more details.
// read it into a workspace with
// file_read('/path/to/file/sample.ini', 2);

[owner]

name := 'Fiona Smythe'
organization := 'Big State University/Physics','Big State University/Astronomy'

[database]
/* use IP address in case network
   name resolution is not working */
server= '192.168.1.42' // use local network.

port=1029