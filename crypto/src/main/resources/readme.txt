This maven module has the cryptography classes that require java 8 (and the internal sun apis) to work.
These have to be built with a bunch of special flags and versions of things and should be separated
from the rest of the build. 